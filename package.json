{
  "name": "spoonasia",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "tinacms dev -c \"astro dev\"",
    "start": "npm run build && npx --yes serve@14 dist -l tcp://0.0.0.0:${PORT:-8080}",
    "build": "if [ -n \"$TINA_TOKEN\" ]; then echo '[tina] building admin...'; tinacms build || echo '[tina] ✗ FAILED — /admin will 404 in this build (continuing)'; else echo '[tina] skipped (no TINA_TOKEN)'; fi && astro build",
    "preview": "astro preview",
    "astro": "astro",
    "dev:astro": "astro dev"
  },
  "dependencies": {
    "astro": "^4.16.18",
    "yahoo-finance2": "^3.14.0"
  },
  "devDependencies": {
    "@tinacms/cli": "^2.3.1",
    "tinacms": "^3.8.1",
    "tsx": "^4.22.0"
  }
}
